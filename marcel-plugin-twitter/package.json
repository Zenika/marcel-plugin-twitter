{
  "name": "marcel-plugin-twitter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf ../frontend && cp -r build ../frontend",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
